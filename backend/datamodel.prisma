type Assets {
    id: ID! @unique
    origID: Int!
    url: String!
}
type Imprint {
    id: ID! @unique
    artist: String!
    startDate: String!
    location: String!
    image: String!
    asset_order: [String]
    assets: [Assets]
}
